import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.farisfaisal.fn.tracker',
  appName: 'fntrack-mobile-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
